name: koni_jxl
description: >-
  Pure Dart JPEG XL (JXL) codec — decode .jxl files to pixels and encode
  lossless .jxl, with zero native dependencies on every Dart/Flutter platform.
version: 0.1.0
repository: https://github.com/zenbaku/koni_jxl
issue_tracker: https://github.com/zenbaku/koni_jxl/issues
topics:
  - jpeg-xl
  - jxl
  - image
  - codec

environment:
  sdk: ^3.6.0
resolution: workspace

dev_dependencies:
  crypto: ^3.0.0
  lints: ^5.0.0
  test: ^1.25.0
